Bellman-Ford 单源最短路径算法

负权环：由两个及两个以上的边组成的环，环的权值为负值。
如果一个图没有负权环，从一个点到另一个点的最短路径，最多经过 V 个顶点，有 V-1 条边。
否则，存在顶点经过了两次，即存在负权环。

在算法运行过程中，对一个点的松弛操作，就是找到经过这个点的另外一条路径，多一条边，权值更小。
如果一个图没有负权环，从一个点到另一个点的最短路径，最多经过 V 个顶点，有 V-1 条边。对所有的点进行 V-1 次松弛操作。
对所有点进行 V-1 次松弛操作，理论上就找到了源点到其他点的最短路径。如果还可以继续松弛，则说明有了负权环。